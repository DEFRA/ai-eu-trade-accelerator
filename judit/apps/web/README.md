# Judit web workbench

Current role:

- operator workbench for inspection plus controlled mutation workflows
- source registry actions (register/search/refresh/discover/register candidates)
- async run jobs (registry runs, dataset comparisons, equine corpus runs)
- review decision persistence
- development reset controls (clear runs / clear all)
- extraction repair workflow triggers
