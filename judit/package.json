{
  "name": "judit-root",
  "private": true,
  "version": "0.1.0",
  "devDependencies": {
    "@marp-team/marp-cli": "^4.1.2",
    "prettier": "^3.6.2"
  },
  "scripts": {
    "decks": "scripts/render_decks.sh",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  }
}
